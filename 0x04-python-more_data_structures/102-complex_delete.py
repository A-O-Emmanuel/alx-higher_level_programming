#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    list_keys   list(a_dictionary.keys())

    for value_dic in list_keys:
        if value    a_dictionary.get(value_disc):
            del a_dictioanry[value_disc]

    return (a_dictionary)
